# Fixture: the named symbol is not near the cited lines of that file

- The security type has `renouncedMint` — `src/other.ts:5-9`. (The display fields at `src/other.ts:20-24` belong to `UserRecord`, a different type.)
