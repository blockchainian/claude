# sample: problem statement

## Accepted when

- **AC1** (unit): an empty wallet yields `[]`.
- **AC1** (integration): `GET /trades` returns 200.

## Facts

- the route exists — `proxy/src/trades.ts:12`
