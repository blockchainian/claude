# sample: problem statement

## Accepted when

- **AC1** (integration): `GET /trades` returns 200 with trades sorted by timestamp desc.
- **AC2** (unit): an empty wallet yields `[]`, not an error.

## Facts

- the route exists — `src/trades.ts:12`
