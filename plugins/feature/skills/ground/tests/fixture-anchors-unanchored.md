# Fixture: a function's behaviour is claimed without a line anchor

- Mapping lives in `src/app.ts`: `parseConfig()` title-cases the raw name.
- `parseConfig` is tested through `ConfigLoader` — `src/app.ts:26-36`.
