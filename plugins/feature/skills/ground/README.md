# ground (pending)

The `ground` skill is not in this plugin yet. It is being changed in place at
`~/Code/blockchainian/dot/.claude/skills/ground/` (installed as
`~/.claude/skills/ground/`), and will be copied here, with `check-paths.sh` and
its `tests/`, once that change lands. Until then `/feature:orchestrate` and the
`planner` agent reference `${CLAUDE_PLUGIN_ROOT}/skills/ground/check-paths.sh`,
which resolves only after the copy.
