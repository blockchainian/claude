{
  "name": "blockchainian-claude",
  "private": true,
  "description": "The blockchainian plugin marketplace for Claude Code: the codex and grok plugins.",
  "type": "module",
  "engines": {
    "node": ">=18.18"
  },
  "scripts": {
    "test": "npm run test:grok && npm run test:codex",
    "test:grok": "node --test tests/*.test.mjs",
    "test:codex": "plugins/codex/skills/execute/tests/test-execute.sh",
    "validate": "claude plugin validate --strict ."
  }
}
